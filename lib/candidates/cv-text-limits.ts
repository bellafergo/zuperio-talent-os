/** Persistencia y autofill: texto crudo extraído del CV. */
export const CV_RAW_TEXT_MAX = 200_000;

/** Campo `cvWorkExperienceText` (formulario, PDF como párrafos recortados). */
export const CV_WORK_EXPERIENCE_FIELD_MAX = 12_000;
