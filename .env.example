# Copy to .env and adjust for your PostgreSQL instance.
# Prisma CLI reads this via prisma.config.ts (dotenv).

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/zuperio_talent_os?schema=public"
